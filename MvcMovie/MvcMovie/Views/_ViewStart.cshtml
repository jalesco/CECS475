@* The _ViewStart.cshtml file defines the common layout all pages will use, so we don't need that delcaration in Index *@
@* It will still be individual views, so any change made in the _Layout will be shown per page.  *@


@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
